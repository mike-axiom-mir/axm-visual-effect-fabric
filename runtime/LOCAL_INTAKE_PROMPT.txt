AXM LOCAL INTAKE — VISUAL EFFECT FABRIC v1.3.0 STEWARDED WORKING HANDOFF

Read in this order:
1. AXM_INTAKE_MANIFEST.json
2. README_FIRST.md
3. ACTION_REPORT.md
4. docs/ARCHITECTURE.md
5. docs/MODULE_PACKAGE_SPEC.md
6. integration/README.md
7. reports/TEST_REPORT.md

Preserve the active direction:
- Fully modular underneath; simple, inspectable controls on top.
- Guide output must remain an ordinary editable recipe.
- Keep module IDs, dependency graphs, provenance, quality tiers, accessibility behavior, and rollback paths explicit.
- Review imports before registry mutation.
- Validate raw recipes before normalization and show normalization changes before confirmation.
- Require sealed-package SHA-256 integrity; reject cycles, unsafe renderer inputs, and unbounded graphs.
- Commit package imports transactionally so rejection leaves the registry unchanged.
- Never flatten composites or silently replace built-ins.
- Repair only through reported bounded changes with a protection snapshot.
- Preserve custom module definitions with snapshots.
- Do not assign a public licence or claim source rights without Mike's explicit decision.
- Treat imported packages as declarative data; executable renderer logic belongs in reviewed adapters.
- Use the canonical resolved-visual-intent contract and report target support explicitly.
- Extend through adapters and versioned modules instead of rebuilding the working foundation.

First local action:
Review the packaged v1.3 evidence, then run `npm run verify` again whenever source changes. Browser smoke and regression paths require their documented Playwright and Chromium dependencies. Current v1.3 browser evidence is packaged; retained v1.2 screenshots and logs are historical only. Without browser tooling, open `OPEN_STUDIO.html`, use Guide, inspect Graph and Performance, validate, and record any newly unverified browser scope honestly.

Highest-value next branch:
Integrate AetherFX into one real AXM product surface and document only the adapter gaps discovered in actual use. Do not start another isolated volume-growth run.
