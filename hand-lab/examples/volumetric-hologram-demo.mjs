import fs from 'node:fs';import path from 'node:path';import {fileURLToPath} from 'node:url';
import {createHandRegistry,executeHandGraph} from '../src/hand-runtime.mjs';import {VOLUMETRIC_HOLOGRAM_HANDS,VOLUMETRIC_HOLOGRAM_GRAPH,makeVolumetricHologramInitialState} from '../src/volumetric-hologram-hands.mjs';
const registry=createHandRegistry(VOLUMETRIC_HOLOGRAM_HANDS);const run=executeHandGraph({registry,graph:VOLUMETRIC_HOLOGRAM_GRAPH,initialState:makeVolumetricHologramInitialState(),context:{callerKind:'deterministic-program'}});const here=path.dirname(fileURLToPath(import.meta.url));const out=path.resolve(here,'../out');fs.mkdirSync(out,{recursive:true});fs.writeFileSync(path.join(out,'volumetric-hologram.html'),run.finalState.realizations.webgl.content);fs.writeFileSync(path.join(out,'volumetric-hologram-state.json'),JSON.stringify({...run,finalState:{...run.finalState,realizations:{webgl:{...run.finalState.realizations.webgl,content:'[derived html omitted]'}}}},null,2));console.log(`VOLUMETRIC_HOLOGRAM_OK stages=${run.executedStageIds.length} hash=${run.finalStateHash}`);
